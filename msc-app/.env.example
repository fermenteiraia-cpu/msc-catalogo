# Public (client-side, prefixadas com VITE_)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Secrets (NUNCA aqui — vão como Supabase secrets nas Edge Functions)
# OPENAI_API_KEY, ANTHROPIC_API_KEY, TERASOFT_USER, TERASOFT_PASS
# Configure via: supabase secrets set OPENAI_API_KEY=... (CLI) ou no painel Supabase
